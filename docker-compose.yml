version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      # Optional: mount data directory to persist database
      - ./server/data:/app/data
    restart: unless-stopped



